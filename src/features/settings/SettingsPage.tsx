import { PageStub } from '@/components/PageStub';

export function SettingsPage() {
  return (
    <PageStub title="Settings">
      <p>OpenRouter API key, models, preferences and export/import arrive in M1.</p>
    </PageStub>
  );
}
